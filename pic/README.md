# PIC

Edit `submission.py`, then:

```bash
python run.py          # score across all generators
python visualize.py    # side-by-side image comparison
```

Change `PLAYER_ONE` / `PLAYER_TWO` in either file to swap strategies.
